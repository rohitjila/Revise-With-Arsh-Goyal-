n=len(arr)
    output=""
    i=0
    while(i < n):
        output+=arr[i]
        cnt=1
        while(i+1 < n and arr[i] == arr[i+1]):
            cnt+=1
            i+=1
        output+=str(cnt)
        i+=1
    return output
